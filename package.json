{
  "name": "malix",
  "version": "0.22.7",
  "description": "npx card for Martin Alix (@malix)",
  "main": "/bin/card.js",
  "bin": {
    "malix": "./bin/card.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/malix/card.git"
  },
  "scripts": {
    "prepublish": "npm run build",
    "build": "node build.js",
    "dev": "npm run build && node ./bin/card.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "card",
    "npm",
    "npm card",
    "npx",
    "npx card",
    "business card",
    "malix"
  ],
  "author": "malixsys",
  "engines": {
    "node": ">=6"
  },
  "devDependencies": {
    "boxen": "^2.1.0",
    "chalk": "^2.4.1"
  }
}
